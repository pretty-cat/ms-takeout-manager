application:
  name: eureka-server2

server:
  port: 8762

eureka:
  instance:
    hostname: eureka-sever2
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka-server1:8761/eureka/